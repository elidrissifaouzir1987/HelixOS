# PLAN-003 controlled Mac mini M4 benchmark evidence

- **Recorded**: 2026-07-11
- **Source commit**: `3355639837f9523b563d2084c0b78f60be5f6c60`
- **Source state**: exact clean detached worktree at benchmark startup
- **Host**: Mac mini (`Mac16,10`), Apple M4 10-core CPU, 16 GB RAM
- **OS**: macOS 26.0, build `25A354`, arm64
- **Storage**: internal solid-state storage, local APFS Data volume
- **Filesystem assurance**:
  `validated-local-APFS-internal-SSD-lock-create-hardlink-sync-primitives`
- **Toolchain**: rustc 1.96.1 (`aarch64-apple-darwin`, LLVM 22.1.2)
- **SQLite**: 3.53.2 via rusqlite 0.40.1/libsqlite3-sys 0.38.1, bundled source
- **Evidence class**: controlled physical Mac mini M4 process-commit evidence; not
  `F_FULLFSYNC`, sector-loss, power-cut or power-loss evidence

The benchmark ran on AC power under `caffeinate` against a new create-only root on the
validated internal APFS volume. The native root was intentionally not retained in the
public evidence. The command used the locked release example with 500 warmups and
10,000 samples, plus the two required bounded public environment labels. It exited 0.

The executed command shape, with only the native create-only paths redacted, was:

```shell
HELIX_BENCH_HARDWARE='Mac mini M4 10-core CPU 16GB internal SSD' \
HELIX_BENCH_FILESYSTEM_ASSURANCE='validated-local-APFS-internal-SSD-lock-create-hardlink-sync-primitives' \
caffeinate -dimsu cargo run --locked --release \
  -p helix-replay-sqlite \
  --example durable_replay_benchmark -- \
  --root '<new-local-APFS-root>' \
  --warmups 500 \
  --samples 10000 \
  --output '<new-evidence-json>'
```

## SC-007 sequential durable claims

Each measurement includes eligibility evaluation, a fresh connection, `BEGIN
IMMEDIATE`, a WAL/FULL commit, connection close and acknowledgement. The fixture is
prepared outside the measured interval.

| Metric | Result | Limit | Status |
|---|---:|---:|---|
| p50 | 0.449792 ms | informational | recorded |
| p95 | 0.528500 ms | 25 ms | pass |
| p99 | 0.583250 ms | 100 ms | pass |
| max | 4.169542 ms | informational | recorded, not discarded |
| measured winners | 10,000 | 10,000 | pass |
| final durable claims/generation | 10,500 / 10,500 | exact | pass |

The store reopened successfully, full integrity passed and the post-reopen exact and
two-axis conflict probes did not mutate the final count. The JSON retains all 10,000
sorted nanosecond samples and records `worktree_clean_at_start: true` plus the exact
source commit. Corpus, expected-outcome, store-schema and backup-manifest digests match
the pinned PLAN-003 values.

## SC-004 held-writer deadline

The controlled busy cap is 40 ms with a 50 ms scheduler tolerance, so every observed
call must return within 90 ms and no detached claim may appear later.

| Scenario | Elapsed | Limit | Status |
|---|---:|---:|---|
| bounded unavailable | 6.651708 ms | 90 ms | pass |
| no detached row | 6.640417 ms | 90 ms | pass |

All six release deadline tests passed. The second scenario released the blocker and
reopened the store to prove that no row appeared after the denied call returned.

## Complementary release workloads

- Contention: four thread scenarios x 100 rounds x 64 contenders passed; 20 process
  rounds x 8 contenders produced exactly one durable winner per round.
- Process kill: five parent matrices passed across the frozen initialization, claim,
  checkpoint, backup and restore boundaries. The sixth reported test is the private
  child-process entry point and returns without work when no worker environment is set.
- Backup/restore: 15 public tests passed; the single ignored item is only the private
  child-process entry point.

These workloads ran sequentially on the same controlled host after the benchmark. They
are correctness/process-kill evidence and are not relabeled as latency, `F_FULLFSYNC`
or power-loss evidence.

## Raw immutable artifacts

| Artifact | Bytes | SHA-256 |
|---|---:|---|
| `benchmark-mac-mini-m4-3355639837f9523b563d2084c0b78f60be5f6c60.json` | 143386 | `d31fa6d648b788466eef9fafbdc030242096cf2a519e66afb8c9d539a737b402` |
| `deadline-mac-mini-m4-3355639837f9523b563d2084c0b78f60be5f6c60.stdout.txt` | 816 | `786e637c97a08c9ab38bb6d6df90452cca39a683f013dc3049ad64043639ad29` |
| `deadline-mac-mini-m4-3355639837f9523b563d2084c0b78f60be5f6c60.stderr.txt` | 141 | `398d81289a27cbdfb03f2daf66397ab3b072a9b284737ad16d26d49530bdc04b` |
| `contention-mac-mini-m4-3355639837f9523b563d2084c0b78f60be5f6c60.stdout.txt` | 342 | `4fd39974b0444ecd8e8322004d64a8efe85a1354ccdf7a500c7bd8ddbbc5cfe7` |
| `contention-mac-mini-m4-3355639837f9523b563d2084c0b78f60be5f6c60.stderr.txt` | 145 | `a0bcad3ed065b098bad5ff5bec3c0c9138c4484eb7f79557083f5ea1e7b8e4df` |
| `process-kill-mac-mini-m4-3355639837f9523b563d2084c0b78f60be5f6c60.stdout.txt` | 499 | `5129a2ff0c69b8d64a324e4df89588062acd1ccd532eff19d2e20f204d37813b` |
| `process-kill-mac-mini-m4-3355639837f9523b563d2084c0b78f60be5f6c60.stderr.txt` | 151 | `2939af29a92b307eceeeb17d1b276306ad058c12b79796c44c32634c94146c33` |
| `backup-restore-mac-mini-m4-3355639837f9523b563d2084c0b78f60be5f6c60.stdout.txt` | 1386 | `099e4a44448d097abcf428b7cd39aa83f551b881cbb5421e2a48cd7853c8ba7f` |
| `backup-restore-mac-mini-m4-3355639837f9523b563d2084c0b78f60be5f6c60.stderr.txt` | 153 | `6c9f8fba75d0b09591cfe540259a30bd9f67de04683862d193c08f0e7c6c6ccd` |

The raw files contain no native replay-root path, secret, operation identifier, nonce,
binding digest or provider diagnostic. Any rerun must use new create-only paths and be
retained independently rather than replacing this result. Repository attributes pin
the hashed inputs and evidence directory to LF so checkout conversion cannot change
their recorded bytes.

## Remaining evidence

This completes T055's physical Mac mini M4 controlled latency probe. The separate
`F_FULLFSYNC` spike and power-cut/power-loss evidence remain explicitly pending. A
manual controlled run also does not establish an automated physical-device runner or
change PLAN-003's aggregate `pending-evidence` status.
