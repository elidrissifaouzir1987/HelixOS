# PLAN-006 Golden Fixture Staging Directory

This tracked placeholder keeps the versioned `golden/` directory present before the
reviewed synthetic canonical bytes and digests are added by later PLAN-006 tasks.

It contains no signed authority, private key, seed, bearer value, acceptance result or
conformance evidence. The placeholder may remain beside later fixtures so a fresh
clone preserves this ownership and nonclaim boundary.
